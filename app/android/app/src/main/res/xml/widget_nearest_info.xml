<?xml version="1.0" encoding="utf-8"?>
<!-- Half an hour is the shortest period Android honours; the app refreshes
     the widget itself on every dataset write and settings change. -->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:description="@string/widget_description"
    android:initialLayout="@layout/widget_nearest"
    android:previewLayout="@layout/widget_nearest"
    android:previewImage="@mipmap/ic_launcher"
    android:minWidth="110dp"
    android:minHeight="80dp"
    android:targetCellWidth="2"
    android:targetCellHeight="2"
    android:minResizeWidth="110dp"
    android:minResizeHeight="40dp"
    android:resizeMode="horizontal|vertical"
    android:updatePeriodMillis="1800000"
    android:widgetCategory="home_screen" />
